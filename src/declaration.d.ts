type CommonState = {
    windowWidth: number
    windowHeight: number
    isMobile: boolean
}

type PictureState = {
    selectedPicture: object,
    isOpenDialog: boolean
}

type ResasState = {
    data: object
}